# Conteúdo em TypeScript (aposentado)

Estes arquivos foram a fonte do guia até a migração para `src/data/guia.json`.

Ficaram aqui só como referência: **a fonte viva agora é o JSON**, que o painel
de administração lê e grava. Editar estes arquivos não muda mais o site.

A migração foi conferida renderizando o guia antes e depois: a saída saiu
byte a byte idêntica.
